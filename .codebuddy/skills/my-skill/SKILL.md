---
name: my-skill
description: [待完成：完整且详细地说明此 skill 的功能以及何时使用。包括触发此 skill 的具体场景、文件类型或任务。]
---

# My Skill

## 概述

[待完成：用 1-2 句话说明此 skill 能实现什么功能]

## 如何组织此 Skill

[待完成：选择最适合此 skill 用途的结构。常见模式：

**1. 工作流式**（适合顺序流程）
- 适用于有明确步骤的流程
- 示例：DOCX skill 的 "工作流决策树" → "读取" → "创建" → "编辑"
- 结构：## 概述 → ## 工作流决策树 → ## 步骤 1 → ## 步骤 2...

**2. 任务式**（适合工具集合）
- 适用于 skill 提供不同操作/功能的场景
- 示例：PDF skill 的 "快速开始" → "合并 PDF" → "拆分 PDF" → "提取文本"
- 结构：## 概述 → ## 快速开始 → ## 任务类别 1 → ## 任务类别 2...

**3. 参考/指南式**（适合标准或规范）
- 适用于品牌指南、编码规范或需求说明
- 示例：品牌样式的 "品牌指南" → "颜色" → "字体" → "功能"
- 结构：## 概述 → ## 指南 → ## 规范 → ## 用法...

**4. 能力式**（适合集成系统）
- 适用于 skill 提供多个相互关联功能的场景
- 示例：产品管理的 "核心能力" → 编号的能力列表
- 结构：## 概述 → ## 核心能力 → ### 1. 功能 → ### 2. 功能...

可以根据需要混合使用这些模式。大多数 skill 会组合多种模式（例如，以任务式开始，为复杂操作添加工作流）。

完成后删除整个"如何组织此 Skill"部分——这只是指导说明。]

## [待完成：根据选择的结构替换为第一个主要章节]

[待完成：在此添加内容。参考现有 skill 的示例：
- 技术类 skill 的代码示例
- 复杂工作流的决策树
- 包含真实用户请求的具体示例
- 根据需要引用 scripts/templates/references]

## 资源

此 skill 包含示例资源目录，展示如何组织不同类型的捆绑资源：

### scripts/
可直接运行的可执行代码（Python/Bash 等），用于执行特定操作。

**其他 skill 的示例：**
- PDF skill：`fill_fillable_fields.py`、`extract_form_field_info.py` - PDF 操作工具
- DOCX skill：`document.py`、`utilities.py` - 文档处理的 Python 模块

**适用于：** Python 脚本、Shell 脚本或任何执行自动化、数据处理或特定操作的可执行代码。

**注意：** 脚本可以在不加载到上下文的情况下执行，但 Claude 仍可以读取它们以进行修补或环境调整。

### references/
文档和参考资料，用于加载到上下文中以指导 Claude 的处理过程和思考。

**其他 skill 的示例：**
- 产品管理：`communication.md`、`context_building.md` - 详细的工作流指南
- BigQuery：API 参考文档和查询示例
- 财务：Schema 文档、公司政策

**适用于：** 深入的文档、API 参考、数据库 Schema、综合指南或 Claude 在工作时应参考的任何详细信息。

### assets/
不加载到上下文中的文件，而是用于 Claude 产出的输出内容。

**其他 skill 的示例：**
- 品牌样式：PowerPoint 模板文件（.pptx）、Logo 文件
- 前端构建器：HTML/React 样板项目目录
- 字体排版：字体文件（.ttf、.woff2）

**适用于：** 模板、样板代码、文档模板、图片、图标、字体或任何用于复制或在最终输出中使用的文件。

---

**可以删除不需要的目录。** 并非每个 skill 都需要所有三种类型的资源。
